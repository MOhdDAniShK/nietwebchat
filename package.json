{
    "name": "nietwebchat",
    "version": "1.0.0",
    "description": "NIET Web Chat - Real-time chat with WhatsApp-style features",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "build": "cd client && npm install && npx vite build && cd ../server && npm install",
        "start": "node server/server.js",
        "postinstall": "npm run build"
    }
}